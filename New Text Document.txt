Git is very powerfull system its version control system//

// Git and Github is very different things //
//lets see git commands!

-git --version
give the version of git

-git config--global user.name "name" 
will set the name to show the global , mean it will set on global any one can see ur name

-git config--global user.email "email"

-there is another method also 

-git config --global --edit

--> mkdir will make diractory and cd we can change the directories and 
move around directories
{
    lets go into core
    if we want to make project as git repo 
    first command is 
    -git init =it will init empty repo into that folder
    -git status = will give info about what changes has been done and all
    -git add (file name) = now our file will be tracking by git... by this
    command your repo will be in staging area /
    {
        there is three stages of commiting./
        working directories->git add->staging area -> git commit -> repository

    }
    -git  commit -m "initial commit"
    -git log(will give info about history of commit)

    --to move around different commits and times there is one hash code
    belonging to every commit copy it and write below command
    -git checkout (hashcode)
}
